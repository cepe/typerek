# Keep the build context small and prevent host artifacts from clobbering the
# image (e.g. node_modules is installed via `npm ci` inside the build).
.git
.gitignore
node_modules
frontend/node_modules
frontend/dist
tmp
log
/public/assets
.env
.env.*
.idea
*.swp
